# My Python Project

This is a simple Python project demonstrating basic functionalities.

## Installation

No special installation steps are required. Simply clone the repository:

```bash
git clone <repository-url>
cd <repository-name>
```

## Usage

To run the main application, execute:

```bash
uv run main.py
```

## Files

- `main.py`: The main script.
- `get_date.py`: A utility script to get the current date.
- `pkg/`: A package containing additional modules (e.g., `calculator.py`).
- `tests.py`: Unit tests for the project.
- `test_calculator.py`: Unit tests specifically for the calculator module.

## btw it was also generated by agent only 😉